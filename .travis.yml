addons:
  apt:
    packages:
    - texlive-latex-base
    - texlive-fonts-recommended
    - texlive-fonts-extra
    - texlive-latex-extra
    - imagemagick
    
before_script:
 - chmod +x .travis/push.sh
 
script:
  - pdflatex cv.tex
  - convert -density 300 cv.pdf -quality 100 cv.png
  
after_success:
  - .travis/push.sh
