dist: precise
addons:
  apt:
    packages:
    - texlive-latex-base
    - texlive-fonts-recommended
    - texlive-fonts-extra
    - texlive-latex-extra
    
before_script:
 - chmod +x .travis/push.sh
 
script:
  - pdflatex cv.tex
  - convert -density 150 cv.pdf -quality 90 -flatten cv.png
  
after_success:
  - .travis/push.sh
